# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
env/
venv/
ENV/
*.egg-info/

# Local virtual environments outside repo (e.g., C:\venvs)
.venvs/
venvs/
dbt_snowflake/
etl_env/

# Environment variables / secrets
.env
.env.*
.envrc

# IDE / Editor settings
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Jupyter / notebook outputs
*.ipynb_checkpoints

# Pytest cache
.pytest_cache/
.cache/

# Airflow logs and config files
logs/
airflow.db
unittests.cfg
airflow.cfg

# Docker volumes & build cache
.docker/
docker-compose.override.yml

# dbt artifacts & user profiles
dbt/target/
dbt/dbt_modules/
dbt/logs/
dbt/.dbt/
dbt/manifest.json
dbt/run_results.json

# Snowflake local credential files (if any)
*.p12
*.pem
*.key

# SnowSQL CLI config and session files
.snowsql/

# CLI log output
*.log

# Shell/CLI history files (if manually saved)
*.history

# Dataset files (if generated locally)
include/**/*.csv
include/**/*.parquet
include/**/*.json

# 👉 Exclude large CSV file from Git tracking (pmo)
include/raw/pmo.csv

# Sandbox experiments
sandbox/

# Project-specific markdown / notes
developer_notes.md

# Diagram exports (optional if files are >5MB)
diagrams/**/*.png
diagrams/**/*.gif
